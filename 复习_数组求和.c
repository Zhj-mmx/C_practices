#include <stdio.h>

int main() {
    int n;
    int sum = 0;
    int a = 1;

    scanf("%d", &n)

        for (int i = 1; i <= n; i++) {
            sum += i * a;
            a = -a
        }
    printf("%d", sum)

        return 0;
}